language: csharp
dist: trusty
sudo: required
mono: none
dotnet: 1.0.1
services:
  - docker

before_install:
  - chmod +x build.sh

script:
  - ./build.sh

 deploy:
  provider: script
  script: ./deploy.sh $DOCKER_USERNAME $HEROKU_AUTH_TOKEN $APP_NAME
  on:
   branch: master